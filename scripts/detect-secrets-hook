#!/usr/bin/env bash
# ---------------------------------------------------------
# detect-secrets-hook – wrapper for the detect-secrets scanner
# ---------------------------------------------------------
set -euo pipefail

# If you use Poetry, you can activate it here:
#   poetry run detect-secrets "$@"

# Otherwise, just call the installed binary:
uv run detect-secrets scan "$@"